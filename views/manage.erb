<a href="/home">
  <div class="header manage">
    <h1><i class="fas fa-edit"></i> Manage</h1>
  </div>
</a>

<div class="content">
  <table class="manages container">
    <tr>
      <th>Active</th>
      <th>Workout</th>
      <th>Public</th>
      <th></th>
    </tr>
    <tr>
      <form action="/workout/add" method="post" >
        <td><input name="day" class="workout-form" type="checkbox" /></td>
        <td><input name="day" class="workout-form full-width" type="text" /></td>
        <td><input name="day" class="workout-form" type="checkbox" /></td>
        <td class="action add">
          <button type="submit">
            <i class="fas fa-plus-circle"></i>
          </button>
        </td>
      </form>
    </tr>
    <% @workouts.each do |workout| %>
    <tr>
      <td class="action gray"><i class="<%= active_workout(workout[:public]) %>"></i></td>
      <td><%= workout[:name] %></td>
      <td class="action gray"><i class="<%= public_workout(workout[:public]) %>"></i></td>
      <td class="action"><i class="fas fa-edit"></i></td>
    </tr>
    <% end %>
  </table>
</div>